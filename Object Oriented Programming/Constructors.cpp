#include<iostream>
int main(){
 /*
 In C++, a constructor is a special member function that is automatically called
  when an object of a class is created.
  Its primary purpose is to initialize the object's data members and allocate resources,
   ensuring the object starts its life in a valid state.
 */


    return 0;
}